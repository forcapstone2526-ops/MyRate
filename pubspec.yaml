name: my_rate
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0 
  intl: ^0.19.0 # FIX: Added the Internationalization package for DateFormat
  # --- ADD FIREBASE PACKAGES HERE ---
  firebase_core: ^2.17.0        # Core Firebase SDK
  firebase_auth: ^4.10.7        # Authentication service
  cloud_firestore: ^4.9.7      # Database service (Firestore) 
  image_picker: ^1.0.7   # ------------------------------------
  cupertino_icons: ^1.0.2
  firebase_storage: ^11.7.7
  provider: ^6.0.5
  mobile_scanner: ^4.0.0  # <--- FOR SCANNING QR CODES
  url_launcher: ^6.3.0  # <--- REQUIRED FOR QR CODE LINKS 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

assets:
    - assets/
